import { convertObjValuesToJSON } from "./convertObjValuesToJSON";
import { requiredConfigName } from "./requiredConfig";
import { resolveLocal } from "./resolveLocal";
import { servedSepOS } from "./servedSepOS";

export const KTools = {
    convertObjValuesToJSON,
    requiredConfigName,
    resolveLocal,
    servedSepOS,
};
